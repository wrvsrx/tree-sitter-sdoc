这是开头的纯文本段落。

{title "文档标题"}

这里是另一个纯文本段落，介绍内容。

{paragraph "这是一个显式段落块，使用字符串形式"}

{paragraph
  这是多行显式段落
  可以包含多行文本
  甚至可以包含 {bold "粗体"} 内容
}

{list
  {item "列表项 1"}
  {item {paragraph "列表中的显式段落"}}
  {item "普通列表项"}
}

最后一个纯文本段落。